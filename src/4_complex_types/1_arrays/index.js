/**
 * There are some kinds of "data" types in JavaScript composed
 * by smaller data pieces. One of they are arrais.
 * Arrays are named "lists" in other programming languajes.
 * JavaScript arrays are ordered lists of values of any kind represented by [], like
 * let fruits = ["apple", "orange", "banana"]
 * 
 * Exercise:
 * - Declare a variable named "primes" and assign to it
 * a array containin the five first prime numbers: 2, 3, 5, 7, 13
 */

// Write your code here.

/**
 * Don't to change next lines.
 */
module.exports = { primes }