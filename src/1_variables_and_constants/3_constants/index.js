/**
 * "const" reserved word defines a "constant". Constants are like variables, but 
 * it's not possible to reassign a constant value. They are "inmutables".
 * Set oranges price so total will be 42
 */

const orangesPrice = 0
const applesPrice = 12

const total = applesPrice + orangesPrice

/**
 * Don't change next lines.
 */

module.exports = { applesPrice, orangesPrice, total }