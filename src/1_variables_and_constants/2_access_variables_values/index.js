/**
 * You can use a variable name in place his value, any way.
 * 
 * Exercice:
 * - Declare a new variable named "myVariable" and assign they 42 value
 * using "secondVariable" for this.
 */

let firstVariable = 42
let secondVariable = firstVariable // secondVariable value is 42

// Write your code here.
let myVariable = secondVariable

/**
 * Don't to change next lines.
 */
module.exports = {
    firstVariable,
    secondVariable,
    myVariable,
}